<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>DonutMarket - Minecraft Shop</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.2/css/all.min.css">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body class="font-sans">

    <div class="container mx-auto p-4 md:p-8 max-w-7xl">

        <header class="flex justify-between items-center mb-12">
            <h1 class="text-3xl font-bold text-white">🍩 DonutMarket</h1>
            <div>
                <% if (user) { %>
                    <div class="flex items-center gap-4">
                        <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png" alt="User Avatar" class="w-10 h-10 rounded-full">
                        <div>
                            <p class="font-semibold"><%= user.username %></p>
                            <p class="text-xs text-gray-400">ID: <%= user.id %></p>
                        </div>
                        <a href="/logout" class="text-gray-400 hover:text-white transition-colors" title="Logout">
                            <i class="fas fa-sign-out-alt"></i>
                        </a>
                    </div>
                <% } else { %>
                    <a href="/auth/discord" class="btn-discord font-bold py-2 px-4 rounded-lg flex items-center gap-2">
                        <i class="fab fa-discord"></i> Login with Discord
                    </a>
                <% } %>
            </div>
        </header>

        <main>
             <section id="how-it-works" class="mb-16">
                 <h2 class="text-2xl font-bold text-center mb-8">How it works? In 3 simple steps.</h2>
                 <div class="grid md:grid-cols-3 gap-8 text-center">
                     <div class="info-box p-6 rounded-lg">
                         <div class="text-3xl font-bold text-indigo-400 mb-2">1.</div>
                         <h3 class="text-xl font-semibold mb-2">Choose Product</h3>
                         <p class="text-gray-400">Select your desired Minecraft spawners from our shop.</p>
                     </div>
                     <div class="info-box p-6 rounded-lg">
                         <div class="text-3xl font-bold text-indigo-400 mb-2">2.</div>
                         <h3 class="text-xl font-semibold mb-2">Login & Purchase</h3>
                         <p class="text-gray-400">Login securely via Discord and complete your purchase. No account creation needed.</p>
                     </div>
                     <div class="info-box p-6 rounded-lg">
                         <div class="text-3xl font-bold text-indigo-400 mb-2">3.</div>
                         <h3 class="text-xl font-semibold mb-2">Receive Delivery</h3>
                         <p class="text-gray-400">Your items will be delivered automatically via in-game bot or support.</p>
                     </div>
                 </div>
             </section>

            <section id="shop" class="mb-16">
                 <h2 class="text-3xl font-bold mb-8">Our Shop</h2>
                 <div class="flex justify-center">
                     <!-- Donut Skeleton Spawner -->
                     <div class="product-card rounded-lg p-6 flex flex-col max-w-md">
                         <div class="relative">
                             <img src="https://placehold.co/400x300/1f2937/ffffff?text=Skeleton+Spawner" alt="Donut Skeleton Spawner" class="rounded-md mb-4">
                             <span class="absolute top-2 right-2 bg-red-500 text-white text-xs font-bold px-2 py-1 rounded-md">NEW</span>
                         </div>
                         <h3 class="text-xl font-semibold mb-2">Donut Skeleton Spawner</h3>
                         <p class="text-sm text-gray-400 mb-4">High-quality spawner for XP farms</p>
                         <p class="text-2xl font-bold text-indigo-400 mb-4">0,50 € each</p>
                         <% if (user) { %>
                             <a href="/product/donut-skeleton-spawner" class="btn-primary w-full py-2 rounded-lg font-semibold mt-auto text-center">Buy Now</a>
                         <% } else { %>
                             <a href="/auth/discord" class="btn-primary w-full py-2 rounded-lg font-semibold mt-auto text-center">Login to Buy</a>
                         <% } %>
                     </div>
                 </div>
             </section>
        </main>
    </div>
</body>
</html>